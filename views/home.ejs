<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>url shortener</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css">
    <style>
        body{
            
            background-color: cornflowerblue;
        }
        .card-header{
            background-color: blue;
            color :black
        }
        .card-body{
            background-color: blueviolet;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12  text-center">
                <h1>URL Shortener</h1>
            </div>
        </div>

        <form action="/create" method="POST">
        <div class="input-group mb-3">
  <input type="text" class="form-control" name="longurl" placeholder="place your long URL " aria-describedby="button-addon2">
  <input class="btn btn-outline-secondary btn-close-white" type="submit" value="submit">
</div>
</form>
<div class="row">
    <% for(let i=0;i<urlResult.length;i++) {%>
    <div class="col-lg-4 ">
        <div class="card border-secondary mb-3" style="max-width: 18rem ;">
  <div class="card-header">Total Click : <%= urlResult[i].clickCount %></div>
  <div class="card-body">
    <h6 class="card-title">
        <a href="http://localhost:3000/<%= urlResult[i].shortUrl%>" target="_blank">http://localhost:3000/<%= urlResult[i].shortUrl%></a>
    </h6>
    <p class="card-text"><%= urlResult[i].longUrl %></p>
    <a href="/delete/<%= urlResult[i].id %>"class="btn btn-sm btn-danger">Remove</a>
  </div>
</div>
    </div>
    <%} %>
</div>
    </div>
</body>
</html>